let JWT_SECRET,jwt;_3f4‍.x([["default",()=>_3f4‍.o]]);_3f4‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_3f4‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
  // create token
  static sign(payload, expiry = "60s", secret = JWT_SECRET) {
    return jwt.sign(payload, secret, { expiresIn: expiry });
  }

  static verify(token, secret) {
    return jwt.verify(token, secret);
  }
}

_3f4‍.d(JwtService);
