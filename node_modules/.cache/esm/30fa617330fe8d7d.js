let Joi,RefreshToken;_30f‍.x([["default",()=>_30f‍.o]]);_30f‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_30f‍.w("../../models",[["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}]]);


const logoutControllers = {
  async logout(req, res, next) {
    // validate
    const refreshSchema = Joi.object({
      refresh_token: Joi.string().required(),
    });

    const { error } = refreshSchema.validate(req.body);
    if (error) {
      return next(error);
    }

    try {
      await RefreshToken.deleteOne({ refresh_token: req.user.refresh_token });
    } catch (err) {
      return next(new Error("Somthing went wrong!"));
    }
    res.json({ message: "logout successfully!" });
  },
};

_30f‍.d(logoutControllers);
